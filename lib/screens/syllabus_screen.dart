import 'package:flutter/material.dart';

class SyllabusScreen extends StatelessWidget {
  static const String id = 'syllabus-screen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container(child:Center(child: Text('Syllabus Screen'))));
  }
}
